THings i did
1. Made RunUI.java
  This is the client and it calls an instance of group client and
  gets a token. (this was just to show it works)

2. Made Token.java
    Just the custom token class.

1. Added a deleteGroup() method to GroupThread
  Idk if this works, needs to be checked. But it atleast compiles.
